description: Bad Request
content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 400
            message:
              type: string
              example: BadRequestError
            data:
              type: object
              properties:
                message:
                  type: string
                  example: Bad request
                errors:
                  type: array
                  items:
                    type: object
                    properties:
                      message:
                        type: string
                        example: Пароль повинен містити принаймні одну малу літеру, одну велику літеру, одну цифру, один спеціальний символ і бути не менше 8 символів довжиною.
                      path:
                        type: array
                        items:
                          type: string
                        example: [password]
                      type:
                        type: string
                        example: string.pattern.base
                      context:
                        type: object
                        properties:
                          regex:
                            type: object
                          value:
                            type: string
                            example: Aaa123!
                          label:
                            type: string
                            example: password
                          key:
                            type: string
                            example: password
